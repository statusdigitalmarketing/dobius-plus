# Audit Findings — Dobius+
Generated by autonomous repair agent. Confidence threshold: MEDIUM.

## Cycle 1

### CRITICAL

_(None found — no data-destroying or crash-on-every-launch bugs)_

### HIGH

- [x] **HIGH:BUG** `electron/data-service.js:261` — getActiveProcesses missing guard for spaceIdx === -1 when pgrep output line has no space, causing incorrect slice behavior
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Add `if (spaceIdx === -1) return null` guard, then filter nulls from result
  - Resolution: Added guard at line 262. `.filter(Boolean)` already present at end of chain.

- [x] **HIGH:ARCHITECTURE** `src/store/store.js:15-17` — Zustand store defines `stats`, `settings`, `activeProcesses` with setters that are called from ProjectView, but components read these values from local useState hooks (useStats, useSessions) instead — dual state management with stale store data
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Remove unused store fields (stats, settings, setStats, setSettings) since useStats hook manages this data locally. Keep sessions and activeProcesses since Overview.jsx reads them from store.
  - Resolution: Removed `stats`, `settings`, `setStats`, `setSettings` from store. Verified no callers. Kept `sessions`, `activeProcesses` which are actively used.

- [x] **HIGH:BUG** `src/components/Dashboard/Plans.jsx:15` — handleExpand loads config instead of plan file content; calls configLoad() which returns app config, not plan markdown. Missing readFile IPC means plan content is never displayed.
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Add `data:readPlanFile` IPC handler + preload method, wire into Plans.jsx to read actual plan .md content
  - Resolution: Added `readPlanFile()` in data-service.js with input validation, IPC handler in main.js, preload API method, and updated Plans.jsx to call `dataReadPlanFile`.

- [x] **MEDIUM:ARCHITECTURE** `electron/data-service.js:44` — Duplicate `timeAgo` function exists in both `electron/data-service.js:44` and `src/lib/time-ago.js:6` with identical logic
  - Confidence: HIGH
  - Disposition: REVIEW — both copies are needed (main process cannot import from renderer src/). Downgraded to MEDIUM.
  - Resolution: Verified both copies are used — data-service uses it for loadHistory (line 80) and listProjects (line 311). Cannot remove without breaking main process. Acceptable duplication across process boundaries.

- [x] **HIGH:SECURITY** `electron/data-service.js:196` — loadTranscript sessionId is used in file path without validation; a malicious sessionId containing `../` could read files outside ~/.claude/projects/
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Validate sessionId matches expected UUID-like format (alphanumeric + hyphens only) before using in path
  - Resolution: Added `if (!/^[\w-]+$/.test(sessionId)) return [];` validation at top of loadTranscript.

- [x] **HIGH:SECURITY** `electron/config-manager.js:68` — setProjectConfig accepts arbitrary projectPath key without rejecting __proto__ / constructor / prototype, enabling prototype pollution
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Add guard to reject dangerous keys before property assignment
  - Resolution: Added `if (['__proto__', 'constructor', 'prototype'].includes(projectPath)) return;` guard.

### MEDIUM

- [x] **MEDIUM:ARCHITECTURE** `src/lib/themes.js:130` — `getThemeByIndex` exported but never imported anywhere
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Remove the unused export
  - Resolution: Removed `getThemeByIndex` function. Verified no imports exist.

- [x] **MEDIUM:ARCHITECTURE** `electron/config-manager.js:116` — `getConfigPath` exported but never imported anywhere
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Remove the unused export
  - Resolution: Removed `getConfigPath` function. Verified no imports exist.

- [x] **MEDIUM:PERFORMANCE** `electron/main.js:56` — BrowserWindow `resize` and `move` event listeners on mainWindow are never cleaned up; potential memory leak when window is recreated
  - Confidence: MEDIUM
  - Disposition: FIX
  - Fix plan: Window listeners are auto-cleaned when BrowserWindow is destroyed — verify this is the case, otherwise add explicit cleanup
  - Resolution: Verified — Electron BrowserWindow auto-removes all event listeners on destroy. The `saveBounds` callback also guards with `!mainWindow.isDestroyed()`. No leak. No code change needed.

- [x] **MEDIUM:GOAL** `src/components/Dashboard/Plans.jsx:17` — Plans component shows metadata placeholder instead of actual plan content (missing readFile IPC)
  - Confidence: HIGH
  - Disposition: FIX (covered by HIGH:BUG above — same fix)
  - Fix plan: Same as Plans.jsx HIGH:BUG finding
  - Resolution: Fixed as part of Plans.jsx HIGH:BUG resolution above.

## Cycle 2 (8-auditor parallel sweep)

### CRITICAL

- [x] **CRITICAL:SECURITY** `src/components/Project/ProjectView.jsx:89` — handleResumeSession interpolates sessionId into terminal command without validation; malformed sessionId could inject shell commands
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Validate sessionId matches `[\w-]+` pattern before writing to terminal
  - Resolution: Added `if (!session.sessionId || !/^[\w-]+$/.test(session.sessionId)) return;` guard.

- [x] **CRITICAL:BUG** `electron/data-service.js:66` — Timestamp dedup uses `>` instead of `>=`; when timestamps are equal, first entry wins instead of last (most recent file position)
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Change `>` to `>=`
  - Resolution: Changed to `>=` so last-write-wins on equal timestamps.

### HIGH

- [x] **HIGH:SECURITY** `electron/terminal-manager.js:24` — node-pty spawn accepts unvalidated cwd from renderer; arbitrary path could be passed
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Validate cwd exists and is a directory before spawning; fall back to home
  - Resolution: Added fs.statSync validation with isDirectory() check; invalid paths fall back to os.homedir().

- [x] **HIGH:BUILD** `build/icon.icns` — Missing macOS .icns icon file; electron-builder needs it for .app bundles
  - Confidence: HIGH
  - Disposition: FIX
  - Fix plan: Generate from existing icon.png using iconutil
  - Resolution: Generated icon.icns (29KB) from icon.png via sips + iconutil pipeline.

### MEDIUM

- [x] **MEDIUM:PERFORMANCE** `electron/main.js:56-64` + `electron/window-manager.js:63-71` — saveBounds fires on every resize/move event (high-frequency during window drag)
  - Confidence: MEDIUM
  - Disposition: FIX
  - Fix plan: Add 300ms debounce before calling loadConfig/saveConfig
  - Resolution: Added clearTimeout/setTimeout debounce in both files. Combined with saveConfig's own 500ms debounce, bounds save is now double-debounced.

### FALSE POSITIVES (from auditors)

- **build/icon.png missing** — file exists (512x512 RGBA PNG)
- **build-and-install.sh not executable** — file is mode 755
- **window-manager.js:28 null access** — getProjectConfig never returns null
- **useTerminal.js:131 missing fit dependency** — fit is in deps, theme handled by separate effect
- **Terminal write enables arbitrary command execution** — inherent to terminal apps, not a vulnerability

### LOW (SKIP → REMAINING-ITEMS.md)

- [ ] **LOW:DX** `electron/data-service.js:1-349` — God file with 349 lines handling 8+ different data loading functions
  - Confidence: HIGH
  - Disposition: SKIP — splitting would be large refactor, current structure is readable

- [ ] **LOW:PERFORMANCE** `electron/data-service.js:20` — parseJsonl uses synchronous fs.readFileSync; should be async
  - Confidence: MEDIUM
  - Disposition: SKIP — read-only local files, sub-millisecond reads, async conversion is large scope change

- [ ] **LOW:PERFORMANCE** `src/components/Dashboard/Overview.jsx:7` — Multiple .reduce() operations over dailyActivity on every render
  - Confidence: MEDIUM
  - Disposition: SKIP — array is max 14 items, negligible performance impact

- [ ] **LOW:DEPENDENCY** — No unused dependencies found; all deps properly placed
  - Confidence: HIGH
  - Disposition: SKIP — clean

- [ ] **LOW:GOAL** — Active process monitoring (pgrep) is scope creep but beneficial
  - Confidence: MEDIUM
  - Disposition: SKIP — adds value

- [ ] **LOW:SECURITY** `electron/main.js:37-40` — Missing `sandbox: true` in BrowserWindow webPreferences
  - Confidence: HIGH
  - Disposition: SKIP — contextIsolation is enabled, sandbox would break node-pty IPC; acceptable for local desktop app

- [ ] **LOW:TEST** — No test suite exists; all source files lack tests
  - Confidence: HIGH
  - Disposition: SKIP — adding test suite is out of scope for audit cycle
