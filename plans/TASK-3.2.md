# Task 3.2 — Visual polish — responsive grid and animation refinements

## What will change
- `src/components/Dashboard/Agents.jsx`:
  1. Responsive grid: ensure cards work at narrow widths (already using auto-fill)
  2. Card hover: border-color change on hover (accent at 30% opacity)
  3. Stats bar stagger animation (delay 0.05s between each)
  4. Empty state with icon and message + Create Agent button
  5. Button hover states with opacity transitions

## Verification
- `npx vite build` exits 0

## Estimated time
10 minutes
